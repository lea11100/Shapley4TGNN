
results_save: False
results_dir: ''
mcts_saved_dir: ''
load_results: False
use_gpu: True
explainer_name: 'pg_explainer_tg'
explainer_ckpt_dir: ''
debug_mode: False

param:
  wikipedia:
    explanation_level: 'event'
    verbose: True
    train_epochs: 100 # 20
    reg_coefs:
      - 0.5
      - 0.1
    batch_size: 16
    lr: 1e-4
  reddit:
    explanation_level: 'event'
    verbose: True
    train_epochs: 30 # 20
    reg_coefs:
      - 0.5
      - 0.1
    batch_size: 16
    lr: 1e-4
  simulate_v1:
    explanation_level: 'event'
    verbose: True
    train_epochs: 20 # 20, 30, 
    # train_epochs: 2 # 20, 30, 
    reg_coefs:
      - 0.5
      - 0.1
    batch_size: 16
    lr: 1e-4
  simulate_v2:
    explanation_level: 'event'
    verbose: True
    train_epochs: 20 # 20, 30, 
    reg_coefs:
      - 0.5
      - 0.1
    batch_size: 16
    lr: 1e-4
  mooc:
    explanation_level: 'event'
    verbose: True
    train_epochs: 70 # 20, 30,
    reg_coefs:
      - 0.5
      - 0.1
    batch_size: 16
    lr: 1e-4
  reddit_hyperlinks:
    explanation_level: 'event'
    verbose: True
    train_epochs: 50 # 20, 30,
    reg_coefs:
      - 0.5
      - 0.1
    batch_size: 16
    lr: 1e-4


  
  


  
